```
(a)if (ival1 != ival2)
       ival1 = ival2      // 缺少;
   else ival1 = ival2 = 0;
``` 

```
(b)if (ival < minval)
       minval = ival;
       occurs = 1;   // 这一句在if外面 应该使用{}
```

```
(c)if (int ival = get_value())
       cout << "ival = " << ival << endl;
   if  (!ival1) // ival是在第一个if里面定义 这里不能使用
       cout << "ival = 0\n";
```

```
(d)if (ival = 0) // ==
       ival = get_value();
```

